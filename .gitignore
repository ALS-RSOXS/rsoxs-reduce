# Environments / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Reduction outputs
results/
*.dat

# Local, machine-specific config (edit a copy of src/.../default_config.toml).
# The bundled default_config.toml is tracked; the working copy here is not.
/reduction_config.toml
